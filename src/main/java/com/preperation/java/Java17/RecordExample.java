package com.preperation.java.Java17;

public record RecordExample(int id, String name, String role, double salary) {
    // The record automatically generates the constructor, getters, equals(), hashCode(), and toString() methods.

}
